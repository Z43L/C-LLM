#include "include/algebra.h"
#include "include/tensor.h"

void suma_conmutativa(t_token *token)
{

    int i = 0;
    if(token->nextoken->embeding->cols  == token->nextoken->embeding->rows)
    {

        while (i < token->embeding->cols)
        {
            
            
        }
    }
    
}

char **matrix_multiply(t_token *token)
{
	token->embeding[token->embeding->cols];
}
